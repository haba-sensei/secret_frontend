import 'package:flutter/material.dart';

class GlobalLogic extends StatelessWidget {
  const GlobalLogic({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Global Page");
  }
}
